# apt で入れるパッケージの宣言。bootstrap が読む。macOS 側の ~/.Brewfile に対応する。
#
# **apt には Brewfile に相当する標準形式が無い。** サードパーティの `aptfile` や
# `equivs` のメタパッケージという手はあるが、道具を増やすほどの利点が無いので、
# 「1行1パッケージ・`#` はコメント」という素のリストにしてある。得たいのは
# フォーマットではなく「**宣言をスクリプトから分離する**」という Brewfile の性質。
#
# 棚卸しの方法(`brew leaves` の対応物):
#     apt-mark showmanual
# 手で入れたパッケージが出るので、このファイルとの差分を見る。
#
# ここに書くのは「**apt でしか入らない / OS に近い**」ものだけ。
# OS を跨いで同じ版が欲しい CLI は ~/.config/mise/config.toml に置く
# (そうしないと、このファイルと ~/.Brewfile に同じリストを二重に書くことになる)。

# --- ビルド基盤 -------------------------------------------------------------
build-essential
pkg-config
unzip

# --- 基本 -------------------------------------------------------------------
curl
wget
git
zsh

# --- エディタ・ナビゲーション -----------------------------------------------
vim
tmux
# universal-ctags は外した(2026-08-15)。vim-gutentags / tagbar を落としたので
# 呼ぶものが無くなった。
# ファイラは yazi に移行(2026-08-15)。apt に無いので mise が持つ。
# ranger と、そのプレビュー用に並べていた w3m-img / atool / caca-utils /
# lynx / highlight / mediainfo も一緒に外した — yazi は自前でプレビューする。

# クリップボードは tmux の OSC 52(set-clipboard on)に寄せたので xsel は削除
# (2026-08-15)。xsel は X11 専用で、macOS でも WSL でも別のものが要る。

# --- PDF ---------------------------------------------------------------------
# pdftotext。ranger のプレビュー用ではなく、資料の中身を読むのに使うので残す。
poppler-utils
